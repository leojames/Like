
<view class="page">
  <view class="page__hd">
    <text class="page__title">view</text>
    <text class="page__desc">弹性框模型分为弹性容器以及弹性项目。当组件的display为flex或inline-flex时，该组件则为弹性容器，弹性容器的子组件为弹性项目。</text>
  </view>
  <view class="page__bd">
    <view class="section" >
     
    </view>
    <view class="section">
      
      <view class="flex-wrp" style="height: 300px;flex-direction:column;">
      <view animation="{{animation}}" >
           <image class="image" model="aspectFit" src="../../resources/man/1.jpeg" />
     </view>
     <view>
        <button type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}"
          disabled="{{disabled}}" bindtap="like"> Like
        </button>
        
        <button type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}"
          disabled="{{disabled}}" bindtap="dislike"> Dislike
        </button>
<button type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}"
          disabled="{{disabled}}" bindtap="reset"> reset
        </button>

        </view>
      <!--  <view class="flex-item bc_red"><image class="image" model="aspectFit" src="../../resources/man/2.jpeg" /></view>
        <view class="flex-item bc_blue"><image class="image"model="aspectFit"  src="../../resources/man/3.jpeg" /></view>
   -->
      </view>
    </view>
  </view>
</view>
